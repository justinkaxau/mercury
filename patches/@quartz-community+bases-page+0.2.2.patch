diff --git a/node_modules/@quartz-community/bases-page/dist/chunk-Y4KGLVLV.js b/node_modules/@quartz-community/bases-page/dist/chunk-Y4KGLVLV.js
index 4743197..77c3c88 100644
--- a/node_modules/@quartz-community/bases-page/dist/chunk-Y4KGLVLV.js
+++ b/node_modules/@quartz-community/bases-page/dist/chunk-Y4KGLVLV.js
@@ -688,7 +688,7 @@ function simplifySlug(fp) {
 function slugifyFilePath(fp, excludeExt) {
   fp = stripSlashes(fp);
   const ext = getFileExtension(fp);
-  const withoutFileExt = fp.replace(new RegExp(ext + "$"), "");
+  const withoutFileExt = fp.replace(new RegExp((ext ? ("\\" + ext) : "") + "$"), "");
   const finalExt = excludeExt || [".md", ".html", void 0].includes(ext) ? "" : ext;
   let slug2 = _sluggify(withoutFileExt);
   if (endsWith(slug2, "_index")) {
@@ -811,9 +811,24 @@ function transformLink(src, target, opts) {
     return joinSegments(pathToRoot(effectiveSrc), canonicalSlug) + folderTail;
   }
 }
-function slugifyPath(s2) {
-  return s2.split("/").map(
-    (segment) => segment.replace(/\s/g, "-").replace(/&/g, "-and-").replace(/%/g, "-percent").replace(/\?/g, "").replace(/#/g, "").replace(/[<>:"|*]/g, "").toLowerCase()
+function slugifyPath(s) {
+  return s.split("/").map(
+    (segment) => segment
+      .normalize("NFD")
+      .replace(/[\u0300-\u036f]/g, "")
+      .replace(/[đĐ]/g, "d")
+      .replace(/&/g, "-and-")
+      .replace(/%/g, "-percent")
+      .replace(/\?/g, "")
+      .replace(/#/g, "")
+      .replace(/[<>:\"|*]/g, "")
+      .replace(/\s+/g, "-")
+      .replace(/[-_.]+/g, (match) => {
+        if (match.includes("-") || match.includes("_")) return "-";
+        return match;
+      })
+      .replace(/^-+|-+$/g, "")
+      .toLowerCase()
   ).join("/").replace(/\/$/, "");
 }
 function _sluggify(s2) {
