diff --git a/node_modules/@quartz-community/crawl-links/dist/index.js b/node_modules/@quartz-community/crawl-links/dist/index.js
index 5005175..bcff1d9 100644
--- a/node_modules/@quartz-community/crawl-links/dist/index.js
+++ b/node_modules/@quartz-community/crawl-links/dist/index.js
@@ -140,9 +140,24 @@ function transformLink(src, target, opts) {
     return joinSegments(pathToRoot(effectiveSrc), canonicalSlug) + folderTail;
   }
 }
-function slugifyPath(s2) {
-  return s2.split("/").map(
-    (segment) => segment.replace(/\s/g, "-").replace(/&/g, "-and-").replace(/%/g, "-percent").replace(/\?/g, "").replace(/#/g, "").replace(/[<>:"|*]/g, "").toLowerCase()
+function slugifyPath(s) {
+  return s.split("/").map(
+    (segment) => segment
+      .normalize("NFD")
+      .replace(/[\u0300-\u036f]/g, "")
+      .replace(/[đĐ]/g, "d")
+      .replace(/&/g, "-and-")
+      .replace(/%/g, "-percent")
+      .replace(/\?/g, "")
+      .replace(/#/g, "")
+      .replace(/[<>:\"|*]/g, "")
+      .replace(/\s+/g, "-")
+      .replace(/[-_.]+/g, (match) => {
+        if (match.includes("-") || match.includes("_")) return "-";
+        return match;
+      })
+      .replace(/^-+|-+$/g, "")
+      .toLowerCase()
   ).join("/").replace(/\/$/, "");
 }
 function _sluggify(s2) {
