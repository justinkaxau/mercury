diff --git a/node_modules/@quartz-community/utils/dist/index.js b/node_modules/@quartz-community/utils/dist/index.js
index 7024f8d..1e97e51 100644
--- a/node_modules/@quartz-community/utils/dist/index.js
+++ b/node_modules/@quartz-community/utils/dist/index.js
@@ -48,18 +48,29 @@ function getFullSlugFromUrl() {
 function slugifyFilePath(fp, excludeExt) {
   fp = stripSlashes(fp);
   const ext = getFileExtension(fp);
-  const withoutFileExt = fp.replace(new RegExp(ext + "$"), "");
+  const withoutFileExt = fp.replace(new RegExp((ext ? ("\\" + ext) : "") + "$"), "");
   const finalExt = excludeExt || [".md", ".html", void 0].includes(ext) ? "" : ext;
-  let slug = _sluggify(withoutFileExt);
-  if (endsWith(slug, "_index")) {
-    slug = slug.replace(/_index$/, "index");
+  if ([".md", ".html", void 0].includes(ext)) {
+    const parts = withoutFileExt.split("/");
+    const lastSegment = parts[parts.length - 1];
+    if (lastSegment === "index" || lastSegment === "_index") {
+      if (parts.length > 1) {
+        return _sluggify(parts.slice(0, -1).join("/")) + "/index";
+      }
+      return "index";
+    }
+    return _sluggify(lastSegment);
+  }
+  let slug2 = _sluggify(withoutFileExt);
+  if (endsWith(slug2, "_index")) {
+    slug2 = slug2.replace(/_index$/, "index");
   }
-  const segments = slug.split("/");
+  const segments = slug2.split("/");
   if (segments.length >= 2 && segments[segments.length - 1] === segments[segments.length - 2]) {
     segments[segments.length - 1] = "index";
-    slug = segments.join("/");
+    slug2 = segments.join("/");
   }
-  return slug + (finalExt ?? "");
+  return slug2 + (finalExt ?? "");
 }
 function joinSegments(...args) {
   if (args.length === 0) {
@@ -197,7 +208,22 @@ function transformLink(src, target, opts) {
 }
 function slugifyPath(s) {
   return s.split("/").map(
-    (segment) => segment.replace(/\s/g, "-").replace(/&/g, "-and-").replace(/%/g, "-percent").replace(/\?/g, "").replace(/#/g, "").replace(/[<>:"|*]/g, "").toLowerCase()
+    (segment) => segment
+      .normalize("NFD")
+      .replace(/[\u0300-\u036f]/g, "")
+      .replace(/[đĐ]/g, "d")
+      .replace(/&/g, "-and-")
+      .replace(/%/g, "-percent")
+      .replace(/\?/g, "")
+      .replace(/#/g, "")
+      .replace(/[<>:\"|*]/g, "")
+      .replace(/\s+/g, "-")
+      .replace(/[-_.]+/g, (match) => {
+        if (match.includes("-") || match.includes("_")) return "-";
+        return match;
+      })
+      .replace(/^-+|-+$/g, "")
+      .toLowerCase()
   ).join("/").replace(/\/$/, "");
 }
 function normalizeHastElement(rawEl, curBase, newBase) {
diff --git a/node_modules/@quartz-community/utils/dist/path.js b/node_modules/@quartz-community/utils/dist/path.js
index 6b3bb3e..d344e97 100644
--- a/node_modules/@quartz-community/utils/dist/path.js
+++ b/node_modules/@quartz-community/utils/dist/path.js
@@ -45,18 +45,29 @@ function getFullSlugFromUrl() {
 function slugifyFilePath(fp, excludeExt) {
   fp = stripSlashes(fp);
   const ext = getFileExtension(fp);
-  const withoutFileExt = fp.replace(new RegExp(ext + "$"), "");
+  const withoutFileExt = fp.replace(new RegExp((ext ? ("\\" + ext) : "") + "$"), "");
   const finalExt = excludeExt || [".md", ".html", void 0].includes(ext) ? "" : ext;
-  let slug = _sluggify(withoutFileExt);
-  if (endsWith(slug, "_index")) {
-    slug = slug.replace(/_index$/, "index");
+  if ([".md", ".html", void 0].includes(ext)) {
+    const parts = withoutFileExt.split("/");
+    const lastSegment = parts[parts.length - 1];
+    if (lastSegment === "index" || lastSegment === "_index") {
+      if (parts.length > 1) {
+        return _sluggify(parts.slice(0, -1).join("/")) + "/index";
+      }
+      return "index";
+    }
+    return _sluggify(lastSegment);
+  }
+  let slug2 = _sluggify(withoutFileExt);
+  if (endsWith(slug2, "_index")) {
+    slug2 = slug2.replace(/_index$/, "index");
   }
-  const segments = slug.split("/");
+  const segments = slug2.split("/");
   if (segments.length >= 2 && segments[segments.length - 1] === segments[segments.length - 2]) {
     segments[segments.length - 1] = "index";
-    slug = segments.join("/");
+    slug2 = segments.join("/");
   }
-  return slug + (finalExt ?? "");
+  return slug2 + (finalExt ?? "");
 }
 function joinSegments(...args) {
   if (args.length === 0) {
@@ -194,7 +205,22 @@ function transformLink(src, target, opts) {
 }
 function slugifyPath(s) {
   return s.split("/").map(
-    (segment) => segment.replace(/\s/g, "-").replace(/&/g, "-and-").replace(/%/g, "-percent").replace(/\?/g, "").replace(/#/g, "").replace(/[<>:"|*]/g, "").toLowerCase()
+    (segment) => segment
+      .normalize("NFD")
+      .replace(/[\u0300-\u036f]/g, "")
+      .replace(/[đĐ]/g, "d")
+      .replace(/&/g, "-and-")
+      .replace(/%/g, "-percent")
+      .replace(/\?/g, "")
+      .replace(/#/g, "")
+      .replace(/[<>:\"|*]/g, "")
+      .replace(/\s+/g, "-")
+      .replace(/[-_.]+/g, (match) => {
+        if (match.includes("-") || match.includes("_")) return "-";
+        return match;
+      })
+      .replace(/^-+|-+$/g, "")
+      .toLowerCase()
   ).join("/").replace(/\/$/, "");
 }
 function normalizeHastElement(rawEl, curBase, newBase) {
