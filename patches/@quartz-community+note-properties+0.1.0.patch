diff --git a/node_modules/@quartz-community/note-properties/dist/components/index.js b/node_modules/@quartz-community/note-properties/dist/components/index.js
index f3e915c..0b36c4c 100644
--- a/node_modules/@quartz-community/note-properties/dist/components/index.js
+++ b/node_modules/@quartz-community/note-properties/dist/components/index.js
@@ -46,18 +46,29 @@ function simplifySlug(fp) {
 function slugifyFilePath(fp, excludeExt) {
   fp = stripSlashes(fp);
   const ext = getFileExtension(fp);
-  const withoutFileExt = fp.replace(new RegExp(ext + "$"), "");
-  const finalExt = [".md", ".html", void 0].includes(ext) ? "" : ext;
-  let slug2 = _sluggify(withoutFileExt);
-  if (endsWith(slug2, "_index")) {
-    slug2 = slug2.replace(/_index$/, "index");
+  const withoutFileExt = fp.replace(new RegExp((ext ? ("\\" + ext) : "") + "$"), "");
+  const finalExt = excludeExt || [".md", ".html", void 0].includes(ext) ? "" : ext;
+  if ([".md", ".html", void 0].includes(ext)) {
+    const parts = withoutFileExt.split("/");
+    const lastSegment = parts[parts.length - 1];
+    if (lastSegment === "index" || lastSegment === "_index") {
+      if (parts.length > 1) {
+        return _sluggify(parts.slice(0, -1).join("/")) + "/index";
+      }
+      return "index";
+    }
+    return _sluggify(lastSegment);
+  }
+  let slug = _sluggify(withoutFileExt);
+  if (endsWith(slug, "_index")) {
+    slug = slug.replace(/_index$/, "index");
   }
-  const segments = slug2.split("/");
+  const segments = slug.split("/");
   if (segments.length >= 2 && segments[segments.length - 1] === segments[segments.length - 2]) {
     segments[segments.length - 1] = "index";
-    slug2 = segments.join("/");
+    slug = segments.join("/");
   }
-  return slug2 + (finalExt ?? "");
+  return slug + (finalExt ?? "");
 }
 function joinSegments(...args) {
   if (args.length === 0) {
@@ -107,9 +118,24 @@ function splitAnchor(link) {
   const slugged = "#" + slug(bare);
   return [fp, slugged];
 }
-function slugifyPath(s2) {
-  return s2.split("/").map(
-    (segment) => segment.replace(/\s/g, "-").replace(/&/g, "-and-").replace(/%/g, "-percent").replace(/\?/g, "").replace(/#/g, "").replace(/[<>:"|*]/g, "").toLowerCase()
+function slugifyPath(s) {
+  return s.split("/").map(
+    (segment) => segment
+      .normalize("NFD")
+      .replace(/[\u0300-\u036f]/g, "")
+      .replace(/[đĐ]/g, "d")
+      .replace(/&/g, "-and-")
+      .replace(/%/g, "-percent")
+      .replace(/\?/g, "")
+      .replace(/#/g, "")
+      .replace(/[<>:\"|*]/g, "")
+      .replace(/\s+/g, "-")
+      .replace(/[-_.]+/g, (match) => {
+        if (match.includes("-") || match.includes("_")) return "-";
+        return match;
+      })
+      .replace(/^-+|-+$/g, "")
+      .toLowerCase()
   ).join("/").replace(/\/$/, "");
 }
 function _sluggify(s2) {
diff --git a/node_modules/@quartz-community/note-properties/dist/index.js b/node_modules/@quartz-community/note-properties/dist/index.js
index f60f026..fc636cf 100644
--- a/node_modules/@quartz-community/note-properties/dist/index.js
+++ b/node_modules/@quartz-community/note-properties/dist/index.js
@@ -10305,18 +10305,29 @@ function simplifySlug(fp) {
 function slugifyFilePath(fp, excludeExt) {
   fp = stripSlashes(fp);
   const ext = getFileExtension(fp);
-  const withoutFileExt = fp.replace(new RegExp(ext + "$"), "");
-  const finalExt = [".md", ".html", void 0].includes(ext) ? "" : ext;
-  let slug2 = _sluggify(withoutFileExt);
-  if (endsWith(slug2, "_index")) {
-    slug2 = slug2.replace(/_index$/, "index");
+  const withoutFileExt = fp.replace(new RegExp((ext ? ("\\" + ext) : "") + "$"), "");
+  const finalExt = excludeExt || [".md", ".html", void 0].includes(ext) ? "" : ext;
+  if ([".md", ".html", void 0].includes(ext)) {
+    const parts = withoutFileExt.split("/");
+    const lastSegment = parts[parts.length - 1];
+    if (lastSegment === "index" || lastSegment === "_index") {
+      if (parts.length > 1) {
+        return _sluggify(parts.slice(0, -1).join("/")) + "/index";
+      }
+      return "index";
+    }
+    return _sluggify(lastSegment);
+  }
+  let slug = _sluggify(withoutFileExt);
+  if (endsWith(slug, "_index")) {
+    slug = slug.replace(/_index$/, "index");
   }
-  const segments = slug2.split("/");
+  const segments = slug.split("/");
   if (segments.length >= 2 && segments[segments.length - 1] === segments[segments.length - 2]) {
     segments[segments.length - 1] = "index";
-    slug2 = segments.join("/");
+    slug = segments.join("/");
   }
-  return slug2 + (finalExt ?? "");
+  return slug + (finalExt ?? "");
 }
 function joinSegments(...args) {
   if (args.length === 0) {
@@ -10429,9 +10440,24 @@ function transformLink(src, target, opts) {
     return joinSegments(pathToRoot(effectiveSrc), canonicalSlug) + folderTail;
   }
 }
-function slugifyPath(s2) {
-  return s2.split("/").map(
-    (segment) => segment.replace(/\s/g, "-").replace(/&/g, "-and-").replace(/%/g, "-percent").replace(/\?/g, "").replace(/#/g, "").replace(/[<>:"|*]/g, "").toLowerCase()
+function slugifyPath(s) {
+  return s.split("/").map(
+    (segment) => segment
+      .normalize("NFD")
+      .replace(/[\u0300-\u036f]/g, "")
+      .replace(/[đĐ]/g, "d")
+      .replace(/&/g, "-and-")
+      .replace(/%/g, "-percent")
+      .replace(/\?/g, "")
+      .replace(/#/g, "")
+      .replace(/[<>:\"|*]/g, "")
+      .replace(/\s+/g, "-")
+      .replace(/[-_.]+/g, (match) => {
+        if (match.includes("-") || match.includes("_")) return "-";
+        return match;
+      })
+      .replace(/^-+|-+$/g, "")
+      .toLowerCase()
   ).join("/").replace(/\/$/, "");
 }
 function _sluggify(s2) {
